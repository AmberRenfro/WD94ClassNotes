let myName ="Amber";
let friendsName ="Vern";

console.log(myName.length);
console.log(friendsName.length);


if (myName.length > friendsName.length) {
    console.log(`${myName}'s name is longer than ${friendsName}'s`);
} else if (myNameLength <friendsNameLength) {
    console.log(`${friendsName}'s name is longer than ${myName}'s`);
} else {
    (myName.length < friendsName.length)
    console.log(`${friendsName} and ${myName} are the same length`);
} 
{console.log(myName.length - friendsName.length);





